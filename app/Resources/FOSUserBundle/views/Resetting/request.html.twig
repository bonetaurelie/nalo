{% extends "layout.html.twig" %}

{% block title %}
{{ parent() }} - {{ 'resetting.title'|trans({}, 'FOSUserBundle') }}
{% endblock title %}

{% block body %}
    <div class="container">
        <div class="main">
            <div class="row">
                <div class=" text-center">
                    <h1 class="connexion">{{ 'resetting.title'|trans({}, 'FOSUserBundle')|upper }}</h1>
                </div>
            </div>
            <div class="row">
                <div class="col-md-6 col-md-offset-3">
                    <div class="well">
                        <div class="row">
                            <div class="col-lg-8 col-lg-offset-2">
                                {% include "FOSUserBundle:Resetting:request_content.html.twig" %}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock body %}
