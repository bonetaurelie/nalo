{% extends "@OCCore/layout.html.twig" %}
{% block title %}
    {{ parent() }} - Inscription
{% endblock %}
{% block body %}
    <div class="container">
        <div class="main">
            <div class="row">
                <div class="col-lg-8 col-lg-offset-2">
                    <h1 class="inscription text-center">INSCRIPTION</h1>
                    <p class="text-justify">Pour soumettre une observation, il est nécessaire de s'inscrire. Si vous êtes un naturaliste professionnel, vous devrez valider les observations des autres utilisateurs. Cochez la case correspondante lors de votre inscription.</p>

                    {#{% if error %}#}
                        {#<div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>#}
                    {#{% endif %}#}
                </div>
            </div>
            <div class="row">
                <div class="col-lg-6 col-lg-offset-3">
                    <div class="well">
                        <div class="row">
                            <div class="col-lg-8 col-lg-offset-2">
                                {% include "FOSUserBundle:Registration:register_content.html.twig" %}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock body %}