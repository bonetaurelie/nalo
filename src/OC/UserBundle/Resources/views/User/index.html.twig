{% extends 'OCCoreBundle::layout.html.twig' %}

{% block title %}
    {{ parent() }} - Profil
{% endblock %}

{% block body %}
    <div class="container ">
        <div class="main">
            <div class="row">
                <ul id="menu">
                    <li>
                        <p>{{ app.user.lastName }} {{ app.user.firstName }}</p>
                        <p>{{ app.user.getFullNameRole }}</p>
                    </li>
                    <li>
                        <a href="{{ path('oc_user_profil') }}">Profil</a>
                    </li>
                    <li>
                        {#{{ path('oc_core_observations') }}#}
                        <a href="#">Mes observations à valider</a>
                    </li>
                    <li>
                        {#{{ path('oc_core_validations') }}#}
                        <a href="#">Validations</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
    <div class="blockprofil">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="profil text-center">MON PROFIL</h1>
                <h2 class="identite text-center">Identité</h2>
            </div>
        </div>
    </div>
{% endblock %}